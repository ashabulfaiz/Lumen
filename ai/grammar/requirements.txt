# Pin TF/Keras together — model .keras must load with the same major Keras that saved it.
# Conda "base" often ships an older Keras than the one used during training.
tensorflow>=2.16.0,<2.20.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
python-dotenv>=1.0.0
# TensorBoard still needs pkg_resources (removed in setuptools 81+)
setuptools>=65.0.0,<81.0.0
tensorboard>=2.15.0,<2.20.0

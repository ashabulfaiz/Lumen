# Unified AI service (grammar + essay + chatbot).
# Groq API key powers the tutor chatbot (/api/chat) and Groq grammar
# correction (/api/ai/correct). The TensorFlow endpoints do not need it.
GROQ_API_KEY=your_groq_api_key_here
